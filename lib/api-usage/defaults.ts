/** Product defaults, not forecasts or changes to billing consent. */
export const DEFAULT_ACCOUNT_BUDGET = {
  daily_limit: "25", monthly_limit: "250", daily_requests: 100,
  paused: false, allow_complex: true,
};
export const DEFAULT_PLATFORM_REQUESTS = 1000;
