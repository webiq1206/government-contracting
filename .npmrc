# Keep lockfile resolved URLs on the public registry. Replit's package-firewall
# proxy URLs break installs outside Replit and can 403 critical CVE packages
# (for example legacy tar via unpdf→canvas) during Cloud Run publishes.
registry=https://registry.npmjs.org/
replace-registry-host=npmjs
